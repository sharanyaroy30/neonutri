{
  "variant": "tint",  // Keeping your local variant
  "primary": "hsl(200 30% 10%)",  // Using remote's primary color
  "appearance": "dark",  // Using remote's appearance
  "radius": 0.8  // Using remote's radius
}
